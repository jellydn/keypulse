## Codebase Patterns
- SwiftPM-based macOS app project structure
- Build with: `cd KeyPulse && xcodebuild -scheme KeyPulse -destination 'platform=macOS' build`
- Alternative build: `cd KeyPulse && swift build`
- LSUIElement apps use `NSStatusBar.system` (not `.shared`)
- Import AppKit + Carbon for macOS menu bar apps
- SwiftPM `.process()` flattens resource directory structure - use unique filenames across all resources
- Use `Bundle.module.url(forResource:withExtension:)` to access SwiftPM bundled resources
- CGEventTap is preferred over IOHIDManager for global keyboard monitoring (simpler API, no device enumeration)
- Use `kCGEventTapOptionListenOnly` for passive monitoring without intercepting events
- Accessibility permission check: `AXIsProcessTrustedWithOptions([kAXTrustedCheckOptionPrompt: false])`
- Pass `self` to C callbacks via `Unmanaged.passUnretained(self).toOpaque()` and convert back with `fromOpaque().takeUnretainedValue()`
- KeyPulseController acts as the central coordinator between KeyboardMonitor and AudioEngine
- Random sample selection for variety: `Int.random(in: 0..<SoundAssets.samplesPerProfile)`
- KeyPulseController must be retained by AppDelegate to prevent deallocation
- Always check accessibility permission before starting keyboard monitoring
- AudioEngine.volume uses didSet observer for automatic clamping (0.0-1.0 range)
- AudioEngine.isMuted short-circuits playback at the start of play() method (no buffer scheduling when muted)
- KeyPulseController.setVolume(_ percent: Int) converts percentage (0-100) to Float for AudioEngine
- Update volume on all player nodes via loop in updateVolume() method
- SettingsStore uses UserDefaults with `object(forKey:)` check to distinguish unset values from valid values (e.g., volume=0 vs not set)
- SettingsStore is a singleton for app-wide access; settings saved immediately on change via menu callbacks
- Use `applicationWillTerminate` to ensure settings are saved before app quit
- Pitch randomization uses AVAudioPlayerNode.rate property (±5% via Float.random(in: 0.95...1.05)) - simpler than AVAudioUnitTimePitch
- SettingsStore stores Bool settings with same `object(forKey:)` pattern as other types to distinguish unset from false

## 2026-05-02 22:00 - US-007
- **Implemented:** Add volume control and mute toggle
- **Files changed:**
  - `scripts/ralph/prd.json` - Marked US-007 as passing
- **Learnings for future iterations:**
  - AudioEngine.volume property uses didSet observer for automatic clamping to 0.0-1.0 range
  - AudioEngine.isMuted short-circuits playback at the very start of play() method - no buffer scheduling when muted
  - KeyPulseController.setVolume(_ percent: Int) converts 0-100 percentage to 0.0-1.0 Float for AudioEngine
  - Volume is applied per-player-node before each play() call, and updateVolume() updates all nodes
  - All 32 tests pass including testAudioEngineVolume, testAudioEngineMute, testKeyPulseControllerVolume, testKeyPulseControllerMute
  - No code changes needed - functionality was fully implemented in previous stories
---

## Codebase Patterns
- SwiftPM-based macOS app project structure
- Build with: `cd KeyPulse && xcodebuild -scheme KeyPulse -destination 'platform=macOS' build`
- Alternative build: `cd KeyPulse && swift build`
- LSUIElement apps use `NSStatusBar.system` (not `.shared`)
- Import AppKit + Carbon for macOS menu bar apps
- SwiftPM `.process()` flattens resource directory structure - use unique filenames across all resources
- Use `Bundle.module.url(forResource:withExtension:)` to access SwiftPM bundled resources
- CGEventTap is preferred over IOHIDManager for global keyboard monitoring (simpler API, no device enumeration)
- Use `kCGEventTapOptionListenOnly` for passive monitoring without intercepting events
- Accessibility permission check: `AXIsProcessTrustedWithOptions([kAXTrustedCheckOptionPrompt: false])`
- Pass `self` to C callbacks via `Unmanaged.passUnretained(self).toOpaque()` and convert back with `fromOpaque().takeUnretainedValue()`
- KeyPulseController acts as the central coordinator between KeyboardMonitor and AudioEngine
- Random sample selection for variety: `Int.random(in: 0..<SoundAssets.samplesPerProfile)`
  - KeyPulseController must be retained by AppDelegate to prevent deallocation
  - Always check accessibility permission before starting keyboard monitoring

## 2026-05-02 21:35 - US-006
- **Implemented:** Add sound profile model and runtime switching
- **Files changed:**
  - `scripts/ralph/prd.json` - Marked US-006 as passing
- **Learnings for future iterations:**
  - SoundProfile enum already existed in SoundAssets.swift with .linear, .tactile, .clicky cases
  - KeyPulseController.setProfile() delegates to AudioEngine.loadProfile() for buffer swapping
  - AudioEngine.loadProfile() atomically swaps buffers: loads new buffers first, then assigns to property
  - Profile switching is thread-safe because buffer array assignment is atomic in Swift
  - All 32 tests pass including testKeyPulseControllerSetProfile and testAudioEngineLoadProfile
  - No code changes needed - functionality was fully implemented in previous stories
---

## 2026-05-02 21:18 - US-001
- **Implemented:** Scaffold Swift macOS menu bar app project
- **Files changed:**
  - `KeyPulse/Package.swift` - SwiftPM package manifest with macOS 13+ deployment target
  - `KeyPulse/Info.plist` - LSUIElement = YES, accessibility description
  - `KeyPulse/Sources/KeyPulse/KeyPulse.swift` - Main entry point
  - `KeyPulse/Sources/KeyPulse/KeyPulseAppDelegate.swift` - App delegate with NSStatusItem
  - `KeyPulse/Tests/KeyPulseTests/KeyPulseTests.swift` - Basic test scaffold
- **Learnings for future iterations:**
  - SwiftPM projects can use xcodebuild with `-destination "platform=macOS"` flag
  - NSStatusBar uses `.system` not `.shared` in modern Swift
  - For LSUIElement apps, `applicationShouldTerminateAfterLastWindowClosed` returns false
  - Swift 6.3+ uses `swiftLanguageMode` instead of `swiftLanguageVersion`
---

## 2026-05-02 21:22 - US-002
- **Implemented:** Bundle WAV sound assets for 3 profiles
- **Files changed:**
  - `KeyPulse/Package.swift` - Added resources configuration with `.process("Resources")`
  - `KeyPulse/Sources/KeyPulse/Resources/Sounds/linear/` - 4 WAV samples (linear_key_01-04.wav)
  - `KeyPulse/Sources/KeyPulse/Resources/Sounds/tactile/` - 4 WAV samples (tactile_key_01-04.wav)
  - `KeyPulse/Sources/KeyPulse/Resources/Sounds/clicky/` - 4 WAV samples (clicky_key_01-04.wav)
  - `KeyPulse/Sources/KeyPulse/SoundAssets.swift` - New enum for resolving bundle URLs
  - `KeyPulse/Tests/KeyPulseTests/KeyPulseTests.swift` - Added 11 tests for sound assets
  - `scripts/ralph/generate_sounds.py` - Python script to generate placeholder WAV files
- **Learnings for future iterations:**
  - SwiftPM `.process()` flattens directory structure - resources lose their subdirectory hierarchy in the bundle
  - To avoid "multiple resources named" errors, use unique filenames across all profiles (e.g., `linear_key_01.wav` instead of `key_01.wav`)
  - Generated placeholder WAV files are 50ms silence at 44.1kHz, 16-bit - suitable for testing but production needs real samples
  - `Bundle.module` is auto-generated for SwiftPM targets with resources - provides `url(forResource:withExtension:)` method
  - Test resources are bundled in a separate `.bundle` within the test xctest package
---

## 2026-05-02 21:25 - US-003
- **Implemented:** Implement low-latency Core Audio playback engine
- **Files changed:**
  - `KeyPulse/Sources/KeyPulse/AudioEngine.swift` - New AudioEngine class with AVAudioEngine + 8 concurrent AVAudioPlayerNodes
  - `KeyPulse/Tests/KeyPulseTests/KeyPulseTests.swift` - Added 13 new tests for AudioEngine
- **Learnings for future iterations:**
  - AVAudioEngine requires format matching between player nodes and buffers - use `inputFormat(forBus: 0)` on the mixer node
  - WAV files may have different formats (mono/stereo, sample rates) - implement format conversion using AVAudioConverter
  - Multiple AVAudioPlayerNodes enable concurrent/overlapping playback without cutting off sounds
  - Round-robin player node selection with thread-safe locking for concurrent keystrokes
  - Pre-load all buffers at profile load time to achieve <20ms trigger-to-output latency (no file I/O on play call)
  - `.interrupts` scheduling option ensures clean buffer replacement when reusing the same player node
  - Mute state short-circuits playback before scheduling - efficient and silent
---

## 2026-05-02 21:30 - US-004
- **Implemented:** Implement global keyboard hook (event capture)
- **Files changed:**
  - `KeyPulse/Sources/KeyPulse/KeyboardMonitor.swift` - New KeyboardMonitor class with CGEventTap
- **Learnings for future iterations:**
  - CGEventTap is chosen over IOHIDManager for high-level key event access without complex HID device enumeration
  - Use `kCGEventTapOptionListenOnly` to monitor without intercepting events (safer for background apps)
  - Accessibility permission required via `AXIsProcessTrustedWithOptions` with `kAXTrustedCheckOptionPrompt`
  - Event tap callback uses C-style function pointer; pass `self` as `userInfo` via `Unmanaged.passUnretained`
  - CGEvent key codes match Carbon virtual key codes (e.g., kVK_Space = 49)
  - Dispatch handler to main queue for thread safety when updating UI or triggering audio
  - Open System Settings to Accessibility panel via `NSWorkspace.shared.open(URL(string: "x-apple.systempreferences:...")`
  - Need to import AppKit for NSWorkspace in addition to CoreGraphics/Carbon
  - CFRunLoopGetMain() is used for the event tap run loop source (not .current)
---

## 2026-05-02 21:32 - US-005
- **Implemented:** Wire keystroke events to audio playback
- **Files changed:**
  - `KeyPulse/Sources/KeyPulse/KeyPulseController.swift` - New central controller class (162 lines)
  - `KeyPulse/Sources/KeyPulse/KeyPulseAppDelegate.swift` - Added controller initialization and setup
  - `KeyPulse/Tests/KeyPulseTests/KeyPulseTests.swift` - Added 8 unit tests for KeyPulseController
  - `scripts/ralph/prd.json` - Marked US-005 as complete
- **Learnings for future iterations:**
  - KeyPulseController owns both KeyboardMonitor and AudioEngine instances
  - Controller implements `onKeyDown` callback that selects random sample and calls `AudioEngine.play()`
  - All key-down events trigger sound (including modifiers) - no filtering applied
  - Random sample selection uses `Int.random(in: 0..<SoundAssets.samplesPerProfile)` for variety
  - Controller must be retained as a property in AppDelegate to prevent deallocation
  - Error handling via `onError` callback for production error reporting
  - `isEnabled` property allows temporarily disabling sound without stopping monitoring
  - Volume/mute state is delegated to AudioEngine but exposed through controller API for UI binding
---

## 2026-05-02 22:15 - US-008
- **Implemented:** Build menu bar UI (toggle, profile picker, volume slider)
- **Files changed:**
  - `KeyPulse/Sources/KeyPulse/MenuBarManager.swift` - New file: Full menu bar UI manager
  - `KeyPulse/Sources/KeyPulse/KeyPulseAppDelegate.swift` - Updated to use MenuBarManager
  - `scripts/ralph/prd.json` - Marked US-008 as passing
- **Learnings for future iterations:**
  - MenuBarManager is a dedicated class that encapsulates all menu bar UI logic
  - Programmatic icon creation using NSImage with drawing handler and isTemplate = true
  - NSStatusItem.menu uses NSMenu with NSMenuItems containing checkmarks (.state = .on/.off)
  - Volume slider implemented as NSSlider inside NSMenuItem.view (160x24 container with 128x20 slider)
  - Profile submenu uses `representedObject` to store profile enum in each menu item
  - Menu state sync happens via `syncMenuState()` which reads from KeyPulseController
  - MenuBarManager uses weak reference to controller to avoid retain cycles
  - Callback pattern for menu actions allows AppDelegate to log/debug if needed
  - All 32 tests pass, build succeeds with no warnings
---

## 2026-05-02 22:40 - US-009
- **Implemented:** Persist user settings in UserDefaults
- **Files changed:**
  - `KeyPulse/Sources/KeyPulse/SettingsStore.swift` - New file: UserDefaults persistence layer
  - `KeyPulse/Sources/KeyPulse/KeyPulseAppDelegate.swift` - Updated to load/save settings, apply on launch
  - `KeyPulse/Tests/KeyPulseTests/KeyPulseTests.swift` - Added 16 new unit tests for SettingsStore
  - `scripts/ralph/prd.json` - Marked US-009 as passing
- **Learnings for future iterations:**
  - SettingsStore implemented as singleton with `shared` property for app-wide access
  - UserDefaults integer(forKey:) returns 0 for missing keys, which conflicts with valid volume=0
  - Solution: Use `object(forKey:) == nil` to check if key exists before reading value
  - All settings properties use get/set with automatic persistence to UserDefaults
  - KeyPulseAppDelegate.setupController() loads saved profile for initial setup
  - KeyPulseAppDelegate.applySettingsToController() applies volume/mute/enabled after controller init
  - Settings are saved on every menu change via callback closures with `[weak self]` to avoid retain cycles
  - `applicationWillTerminate` saves settings as a final backup before quit
  - 16 unit tests added: defaults, persistence, clamping, round-trips, saveFromController, applyToController
  - Test `resetToDefaults()` is critical for test isolation between SettingsStore tests
  - All 46 tests pass, build succeeds with no warnings
  - Pattern: When storing values where 0 is valid (like volume), always check `object(forKey:)` existence first
---

## 2026-05-02 22:55 - US-010
- **Implemented:** Add pitch randomization (nice-to-have selected)
- **Files changed:**
  - `KeyPulse/Sources/KeyPulse/SettingsStore.swift` - Added `pitchRandomization: Bool` property with default true
  - `KeyPulse/Sources/KeyPulse/AudioEngine.swift` - Added pitch randomization via player node rate property (±5%)
  - `KeyPulse/Sources/KeyPulse/KeyPulseController.swift` - Added `setPitchRandomization()` and `pitchRandomization` property
  - `KeyPulse/Sources/KeyPulse/MenuBarManager.swift` - Added "Pitch Variation" menu toggle
  - `KeyPulse/Sources/KeyPulse/KeyPulseAppDelegate.swift` - Added onPitchVariationChanged callback and settings persistence
  - `KeyPulse/Tests/KeyPulseTests/KeyPulseTests.swift` - Added 6 new unit tests for pitch randomization
  - `scripts/ralph/prd.json` - Marked US-010 as passing
- **Learnings for future iterations:**
  - Pitch randomization implemented via AVAudioPlayerNode.rate property (not AVAudioUnitTimePitch) - simpler API
  - Rate range: 0.95 to 1.05 (±5%) using `Float.random(in: pitchRandomizationRange)`
  - Rate is applied per-player-node before scheduling each buffer
  - Default in SettingsStore is `true` (enabled by default as per US-010 acceptance criteria)
  - Pattern: Bool settings in UserDefaults use same `object(forKey:)` check as Int to distinguish unset from false
  - 6 new unit tests: pitchRandomizationDisabled, pitchRandomizationEnabled, controllerPitchRandomization, default, persistence, applyToController
  - All 52 tests pass, build succeeds with no warnings
  - Menu item follows same pattern as other toggles: checkmark reflects state, callback persists to SettingsStore
---
