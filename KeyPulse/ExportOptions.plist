<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Export options for App Store distribution -->
    <key>destination</key>
    <string>upload</string>

    <!-- Signing method for TestFlight/App Store -->
    <key>method</key>
    <string>app-store</string>

    <!-- Provisioning profile (automatic signing recommended for development) -->
    <key>provisioningProfiles</key>
    <dict>
        <key>com.keypulse.app</key>
        <string>KeyPulse_App_Store</string>
    </dict>

    <!-- Signing -->
    <key>signingCertificate</key>
    <string>Apple Distribution</string>
    <key>signingStyle</key>
    <string>manual</string>

    <!-- App Store Connect team ID (replace with your actual team ID) -->
    <!-- Get this from https://appstoreconnect.apple.com -> Users and Access -> Keys -->
    <key>teamID</key>
    <string>YOUR_TEAM_ID_HERE</string>

    <!-- Thinning for distribution (none = all architectures) -->
    <key>thinning</key>
    <string>&lt;none&gt;</string>

    <!-- Compile bitcode (required for App Store) -->
    <key>compileBitcode</key>
    <true/>

    <!-- Upload symbols for crash reporting -->
    <key>uploadSymbols</key>
    <true/>

    <!-- Upload bitcode -->
    <key>uploadBitcode</key>
    <true/>

    <!-- iCloud container environment -->
    <key>iCloudContainerEnvironment</key>
    <string>Production</string>

    <!-- Distribution bundle identifier -->
    <key>manageAppVersionAndBuildNumber</key>
    <true/>
</dict>
</plist>
